#include <iostream>
using namespace std;

int main(int argc, char** argv)
{
int cuantos;
cout << "“Cuantos datos va a introducir? ";
cin >> cuantos;
if(cuantos <= 0)
cout << "La cantidad no es valida\n";
...
cin >> a >> b >> c;
cout << "El menor es de los tres numeros " << menorde3(a, b, c) << endl;
return 0;
}
int menorde3(int x, int y, int z)
{

return menor(menor(x, y), Zz);

}